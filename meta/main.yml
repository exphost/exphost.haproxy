---
galaxy_info:
  role_name: haproxy
  author: TORGiren
  description: HAproxy load balancer
  company: exphost
  license: MIT
  min_ansible_version: 2.8
  platforms:
    - name: el
      versions:
        - 7
  galaxy_tags:
    - fabsible
    - haproxy

dependencies:
  - role: exphost.create_user
    user: "{{ app.value.haproxy.user }}"
